import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: 150,
    },
    title: {
        fontSize: 20,
        fontWeight: 'bold'
    },
    text: {
        fontSize: 16,
        fontWeight: 'bold'
    },
    yielded: {
        fontSize: 14,
        color: '#00aa00',
        fontWeight: 'bold'
    }
});