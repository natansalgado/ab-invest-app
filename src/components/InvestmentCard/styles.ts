import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 15,
        borderBottomWidth: 1,
        borderColor: '#ccc'
    },
    name: {
        fontSize: 20,
        fontWeight: 'bold',
    },
    label: {
        marginTop: 5,
        fontSize: 16,
    },
    text: {
        fontSize: 18,
        fontWeight: 'bold',
    }
});