import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 15,
        borderBottomWidth: 1,
        borderColor: '#ccc'
    },
    label: {
        fontSize: 18
    },
    total: {
        fontSize: 20,
        fontWeight: 'bold'
    }
});