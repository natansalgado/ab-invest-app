import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    label: {
        padding: 20,
        paddingTop: 10,
        paddingBottom: 2,
        fontSize: 18
    },
    buttons: {
        flexDirection: 'row',
        paddingHorizontal: 20,
        gap: 10
    }
});