import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 20
    },
    image: {
        width: "100%",
        objectFit: 'contain',
        alignSelf: 'center',
        marginTop: 40,
        marginBottom: -10
    },
    buttons: {
        gap: 2
    }
});