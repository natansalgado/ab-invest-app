import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    field: {
        paddingVertical: 15,
        paddingHorizontal: 15,
        borderBottomWidth: 1,
        borderColor: "#ccc"
    },
    fieldText: {
        fontSize: 18
    }
});